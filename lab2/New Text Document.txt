Дараах асуултуудад хариулж тайлан бичиж кодын хамт Тиймсээр илгээнэ.

1. Санах ойн хаяг гэж юу вэ?
2. Хаяган хувьсагч гэж юу вэ? Хаяган хувьсагчийн төрөл ямар байдаг мөн багтаамж нь хэдэн байт байдаг вэ?
3. new оператор гэж юу вэ? ямар үйлдэл хийж юу буцаадаг вэ?
4. Санах ойн цоорхойн талаар дэлгэрэнгүй тайлбарла. Ойн цоорхой гэж юу болох, ямар дохиолдолд үүсэх, яаж сэргийлэх гэх мэт.
5. Доорх кодыг туршиж үр дүнг тайлабарлан бич. 

char *p1;
int *p2;
double *p3;
cout<<sizeof(p1)<<endl<<sizeof(p2)<<endl<<sizeof(p3); //хэд хэд гэсэн утга хэвлэж байна вэ?  Яагаад?

5. Доорх кодыг туршиж  мөр бүрийн үр дүнг тайлбарлан бич.

int a=125;
int *p = &a;
cout<<p<<endl;
cout<<*p<<endl;
p++;
cout<<p<<endl;
cout<<*p<<endl;

6. Доорх кодын мөр бүрийг тайлбарла.

  int numbers[5];
  int * p;
  p = numbers;  *p = 10;
  p++;  *p = 20;
  p = &numbers[2];  *p = 30;
  p = numbers + 3;  *p = 40;
  p = numbers;  *(p+4) = 50;
  for (int n=0; n<5; n++)
    cout << numbers[n] << ", ";

7. Хаяган хувьсагч ашиглан (функцын параметер нь хаяган хувьсагч байна) хоёр хувьсагчийн утгыг солих хэрэглэгчийн функц бич.
8. Заалтан хувьсагч (функцын параметер нь заалт байна) хоёр хувьсагчийн утгыг солих хэрэглэгчийн функц бич.

https://algs4.cs.princeton.edu/lectures/